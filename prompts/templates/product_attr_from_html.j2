你是一名电商商品结构化抽取专家。

【任务】
下面是一段电商商品详情页的 HTML 源码（包含标签和文本内容）。
请你从中抽取指定的商品属性，并输出合法 JSON。

【输入 HTML 源码】
================ HTML BEGIN ================
{{ html_content }}
================ HTML END ==================

【需要抽取的属性】
{% for field in fields %}
- {{ field.name }}：{{ field.desc }}
{% endfor %}

【规则】
1. 只能根据 HTML 中的真实内容判断，不要臆造信息。
2. 若属性在 HTML 中未明确出现，输出 null。
3. 输出必须是合法 JSON，不得包含多余说明文字。

【输出 JSON 示例】
{
  "brand": "良品铺子",
  "flavor": "海苔味",
  "sugar_free": null,
  "target_audience": "儿童"
}

现在请根据上述要求，直接输出 JSON：